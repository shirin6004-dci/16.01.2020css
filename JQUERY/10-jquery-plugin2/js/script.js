const myCircle = new PercentChart("container", {

    data: {
        // 10%
        percent: 10,
        // text & number displayed inside the circle
        actual: 190,
        unit: "TRIALS"
    }

})